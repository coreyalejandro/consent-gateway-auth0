# CLAUDE.md — Consent Gateway (Auth0)

## What This Is

TLC overlay for the Consent Gateway Auth0 integration (git submodule when linked). Inherits the root `CLAUDE.md` and `THE_LIVING_CONSTITUTION.md`. See `BUILD_CONTRACT.md` if present and root verification docs for submodule expectations.

## Domain

Empirical Safety — consent and gateway boundaries.

## Rules

All Living Constitution Articles I–V apply. Keep canonical `00-constitution/` alignment with the parent TLC repo when the submodule is updated.
